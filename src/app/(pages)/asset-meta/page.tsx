import { AssetMetaTable } from './asset-meta-table';

export default async function AssetMetaPage() {
  return (
    <div className="container mx-auto p-8">
      <AssetMetaTable />
    </div>
  );
}
