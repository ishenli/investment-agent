# Tasks: Model Provider Management Feature

**Change ID**: `add-model-provider-management`
**Status**: Draft

---

## Overview

This document breaks down the implementation of the model provider management feature into ordered, verifiable tasks. Each task can be completed independently and produces user-visible progress.

---

## Task List

### T1: Database Schema Implementation

**Description**: Create the database schema for `model_providers` and `provider_models` tables in Drizzle ORM.

**Files to Create/Modify**:
- `drizzle/schema.ts` - Add `modelProviders` and `providerModels` table definitions

**Verification**:
- [ ] Tables are defined with correct columns and types
- [ ] Foreign key relationships are properly configured
- [ ] Indexes are added on `account_id`, `slug`, and `provider_id`
- [ ] `pnpm db:generate` creates migration file

**Acceptance Criteria**:
- Schema passes TypeScript compilation
- Migration file is generated successfully
- Cascade delete is configured for `providerModels`

---

### T2: Database Migration

**Description**: Generate and apply the database migration for the new tables.

**Files to Create/Modify**:
- `drizzle/migrations/` - New migration file generated by Drizzle

**Verification**:
- [ ] Migration file is generated
- [ ] Migration runs without errors
- [ ] Tables exist in database with correct structure

**Acceptance Criteria**:
- Migration applies cleanly in development environment
- New tables are verified via `drizzle-kit studio` or direct query

---

### T3: Service Layer - Model Provider

**Description**: Implement the data access service for model providers.

**Files to Create**:
- `src/server/service/modelProviderService.ts`
- `src/server/service/modelProviderService/__tests__/modelProviderService.test.ts`

**Verification**:
- [ ] `createProvider` - Inserts new provider with validation
- [ ] `getProvidersByAccountId` - Returns providers for account
- [ ] `getProviderById` - Returns single provider
- [ ] `updateProvider` - Updates existing provider
- [ ] `deleteProvider` - Deletes provider and cascades models
- [ ] `setProviderActive` - Toggles provider active status
- [ ] All methods handle errors appropriately

**Acceptance Criteria**:
- Unit tests pass for all methods
- Service throws appropriate errors for invalid inputs
- Cascade delete works correctly

---

### T4: Service Layer - Provider Models

**Description**: Implement the data access service for provider models.

**Files to Create/Modify**:
- `src/server/service/modelProviderService.ts` (extend existing file)
- `src/server/service/modelProviderService/__tests__/modelProviderService.test.ts` (add tests)

**Verification**:
- [ ] `createModel` - Adds model to provider
- [ ] `getModelsByProviderId` - Returns models for provider
- [ ] `getModelById` - Returns single model
- [ ] `updateModel` - Updates existing model
- [ ] `deleteModel` - Removes model from provider

**Acceptance Criteria**:
- Unit tests pass for model methods
- Models correctly linked to provider
- Provider id validation in place

---

### T5: Business Controller - Model Providers

**Description**: Create the business logic controller for model providers API.

**Files to Create**:
- `src/server/controller/modelProvider.ts`

**Verification**:
- [ ] `ModelProviderBizController` extends `BaseBizController`
- [ ] All methods use `@WithRequestContext()` decorator
- [ ] `getProviders` - List providers for current account
- [ ] `createProvider` - Validate with Zod and create provider
- [ ] `updateProvider` - Validate and update provider
- [ ] `deleteProvider` - Delete provider with authorization check
- [ ] All methods return standardized responses via `this.success()` / `this.error()`

**Acceptance Criteria**:
- Controller methods return correct ResultUtil responses
- User authentication enforced via `AuthService`
- Zod validation schemas defined and used

---

### T6: Business Controller - Provider Models

**Description**: Create the business logic controller for provider models API.

**Files to Create/Modify**:
- `src/server/controller/modelProvider.ts` (extend existing file)

**Verification**:
- [ ] `getModels` - List models for provider
- [ ] `createModel` - Validate and add model to provider
- [ ] `updateModel` - Validate and update model
- [ ] `deleteModel` - Remove model with authorization check

**Acceptance Criteria**:
- Provider ownership validation
- Model validation with Zod schemas
- Error handling for non-existent provider

---

### T7: API Routes - Model Providers

**Description**: Create the HTTP layer routes for model providers.

**Files to Create**:
- `src/app/api/model-providers/route.ts`

**Verification**:
- [ ] `ModelProviderHttpController` extends `BaseController`
- [ ] Implements GET, POST, PUT, DELETE static methods
- [ ] Uses `@WithRequestContext()` decorator on all methods
- [ ] Calls business controller and returns `Response.json()`
- [ ] HTTP methods properly exported

**Acceptance Criteria**:
- All endpoints return correct HTTP status codes
- Error responses follow project conventions
- Route is accessible and handles requests

---

### T8: API Routes - Provider Models

**Description**: Create the HTTP layer routes for provider models.

**Files to Create**:
- `src/app/api/model-providers/[id]/models/route.ts`

**Verification**:
- [ ] `ProviderModelsHttpController` extends `BaseController`
- [ ] Implements GET, POST, PUT, DELETE static methods
- [ ] Validates provider ID from URL params
- [ ] Calls business controller and returns `Response.json()`

**Acceptance Criteria**:
- Dynamic route handles provider ID correctly
- Non-existent provider returns 404
- Model operations scoped to correct provider

---

### T9: Store - Initial State

**Description**: Define the initial state for the model provider store.

**Files to Create**:
- `src/app/store/modelProvider/initialState.ts`
- `src/app/store/modelProvider/slices/providers/initialState.ts`
- `src/app/store/modelProvider/slices/models/initialState.ts`
- `src/app/store/modelProvider/slices/form/initialState.ts`

**Verification**:
- [ ] ProvidersState defined with list, activeId, loading, error, saving
- [ ] ModelsState defined with list, providerId, loading, error, saving
- [ ] FormState defined with mode, draftProvider, draftModel, isDirty, errors
- [ ] Combined ModelProviderStoreState exports all three

**Acceptance Criteria**:
- Initial values are appropriate (empty lists, null IDs, false booleans)
- TypeScript types compile without errors

---

### T10: Store - Providers Slice

**Description**: Implement the providers CRUD actions slice.

**Files to Create**:
- `src/app/store/modelProvider/slices/providers/action.ts`
- `src/app/store/modelProvider/slices/providers/selector.ts`

**Verification**:
- [ ] `createProvidersSlice` function defined
- [ ] `fetchProviders` - Fetch from API and update state
- [ ] `createProvider` - Create via API and update state
- [ ] `updateProvider` - Update via API and sync state
- [ ] `deleteProvider` - Delete via API and remove from state
- [ ] `setActiveProvider` - Set active provider ID
- [ ] Loading and error state management
- [ ] Selectors for active provider, active provider models

**Acceptance Criteria**:
- Actions use `produce` where needed for immutability
- Loading states set before and cleared after async ops
- Error states captured on failures

---

### T11: Store - Models Slice

**Description**: Implement the models CRUD actions slice.

**Files to Create**:
- `src/app/store/modelProvider/slices/models/action.ts`
- `src/app/store/modelProvider/slices/models/selector.ts`

**Verification**:
- [ ] `createModelsSlice` function defined
- [ ] `fetchModels` - Fetch models for provider
- [ ] `createModel` - Add model to provider
- [ ] `updateModel` - Update model
- [ ] `deleteModel` - Remove model
- [ ] Loading and error state management
- [ ] Selectors for models count, active models

**Acceptance Criteria**:
- Models linked to selected provider
- State cleared when changing providers

---

### T12: Store - Form Slice

**Description**: Implement the form state management slice.

**Files to Create**:
- `src/app/store/modelProvider/slices/form/action.ts`
- `src/app/store/modelProvider/slices/form/selector.ts`

**Verification**:
- [ ] `createFormSlice` function defined
- [ ] `resetForm` - Clear all form state
- [ ] `setDraftProvider` - Update provider draft
- [ ] `setDraftModel` - Update model draft
- [ ] `setFormMode` - Switch between create/edit modes
- [ ] `setFormError` / `clearFormError` - Manage validation errors
- [ ] `submitProvider` - Call providers slice create/update
- [ ] `submitModel` - Call models slice create/update

**Acceptance Criteria**:
- IsDirty flag set correctly on data changes
- Validation error management works

---

### T13: Store - Main Store

**Description**: Create the main Zustand store for model providers.

**Files to Create**:
- `src/app/store/modelProvider/store.ts`

**Verification**:
- [ ] Store type combines state and actions
- [ ] `createStore` function aggregates initial state and slices
- [ ] `useModelProviderStore` exported with devtools and shallow compare
- [ ] `getModelProviderState` getter for external access

**Acceptance Criteria**:
- Store accessible from components
- Devtools integration works
- Shallow comparison optimization applied

---

### T14: UI - Base Components

**Description**: Create reusable UI components for the model provider interface.

**Files to Create**:
- `src/app/components/model-provider/ProviderListPanel.tsx`
- `src/app/components/model-provider/ProviderConfigPanel.tsx`
- `src/app/components/model-provider/ProviderForm.tsx`
- `src/app/components/model-provider/ModelsSection.tsx`
- `src/app/components/model-provider/ModelListItem.tsx`

**Verification**:
- [ ] Components use existing UI components (Button, Input, Select, etc.)
- [ ] Component structure matches design document
- [ ] Loading states displayed correctly
- [ ] Error messages shown appropriately
- [ ] Empty states with actionable prompts

**Acceptance Criteria**:
- Components render without errors
- Props documentation via TypeScript
- Responsive design with Tailwind classes

---

### T15: UI - Main Page

**Description**: Create the main model providers management page.

**Files to Create**:
- `src/app/(pages)/model-providers/page.tsx`
- `src/app/components/model-provider/index.ts`

**Verification**:
- [ ] Split-pane layout (list on left, config on right)
- [ ] Store integration (useModelProviderStore)
- [ ] Initial data fetch on mount
- [ ] Provider selection handling
- [ ] Form mode switching (create/edit/view)
- [ ] Notification system for success/error messages

**Acceptance Criteria**:
- Page accessible at `/model-providers` route
- All CRUD operations work from UI
- Keyboard navigation support where appropriate

---

### T16: Navigation Integration

**Description**: Add model providers to the app navigation.

**Files to Modify**:
- `src/app/components/app-sidebar.tsx`

**Verification**:
- [ ] Navigation item added for "模型服务商"
- [ ] URL set to `/model-providers`
- [ ] Appropriate icon selected
- [ ] Proper localization if applicable

**Acceptance Criteria**:
- Link appears in sidebar navigation
- Link navigates to correct page
- Active state shows when on page

---

### T17: Type Definitions

**Description**: Define TypeScript types for the feature.

**Files to Create**:
- `src/types/modelProvider.ts`

**Verification**:
- [ ] `ModelProvider` interface (from DB schema)
- [ ] `ProviderModel` interface (from DB schema)
- [ ] `CreateModelProviderRequest` interface
- [ ] `UpdateModelProviderRequest` interface
- [ ] `CreateProviderModelRequest` interface
- [ ] `UpdateProviderModelRequest` interface
- [ ] Form mode type definitions

**Acceptance Criteria**:
- Types match database schema columns
- Request/response types separate from DB types
- All type usages compile

---

### T18: Test Coverage

**Description**: Complete test coverage for all new code.

**Files to Create/Modify**:
- All `__tests__` files in respective directories

**Verification**:
- [ ] Service layer unit tests (≥90% coverage)
- [ ] Controller tests (≥85% coverage)
- [ ] API route integration tests
- [ ] Store action tests
- [ ] Component tests (≥80% coverage)

**Acceptance Criteria**:
- `pnpm test:coverage` meets coverage thresholds
- All tests pass
- No console errors in tests

---

### T19: Documentation

**Description:** Update project documentation for the new feature.

**Files to Create/Modify**:
- `README.md` (if applicable)
- `docs/model-providers.md` (optional)

**Verification**:
- [ ] Feature overview documented
- [ ] API endpoints listed with examples
- [ ] Database schema described
- [ ] Usage instructions for developers

**Acceptance Criteria**:
- Documentation is accurate and helpful
- Code examples provided

---

### T20: Validation and Final Testing

**Description:** End-to-end testing and validation of the complete feature.

**Files to Modify**:
 Various (bug fixes as needed)

**Verification**:
- [ ] Manual testing of all CRUD operations
- [ ] Error scenario testing (invalid data, network errors)
- [ ] Cross-browser testing
- [ ] Accessibility testing (keyboard nav, screen reader)
- [ ] Performance testing (large provider lists)
- [ ] Security testing (unauthorized access attempts)

**Acceptance Criteria**:
- All user scenarios work correctly
- No regressions in existing functionality
- Feature ready for production deployment

---

## Task Dependencies

```
T1 ──► T2
 │
 ├─► T3 ──► T5 ──► T7
 │      │
 │      └─► T6 ──► T8
 │
 └─► T4 ──► T6
       │
       └─► T8

T9 ──► T10 ──► T13
 │
 ├─► T11 ──► T13
 │
 └─► T12 ──► T13

T2 + T13 + T17 ──► T14 ──► T15 ──► T16

T15 + Tests from T3,T4,T5,T6,T7,T8,T10,T11,T12 ──► T18

T7,T8,T10,T11,T12,T15,T18 ──► T20

T20 ──────────────────────────────────────────────► T19
```

---

## Parallelizable Tasks

The following tasks can be worked on in parallel after their dependencies are met:

1. **After T5,T6,T7,T8**: UI development (T14, T15, T16) can proceed in parallel with store work
2. **After T9**: Store slices (T10, T11, T12) can be developed in parallel
3. **After T3,T4,T5,T6**: Tests (T18) can be written alongside implementation